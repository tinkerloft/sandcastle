---
"@ai-hero/sandcastle": patch
---

Add `openSandbox()` isolated sandbox provider using `@alibaba-group/opensandbox` SDK. Includes `stdin` piping support for agent prompt delivery and consistent stderr handling.
